apply plugin: 'org.springframework.boot'

dependencies {
    api("com.baomidou:mybatis-plus-boot-starter") {
        exclude group:"mybatis-spring", module:"org.mybatis"
    }
    api 'org.mybatis:mybatis-spring'
    api project(':faircloud-common-core')
}
